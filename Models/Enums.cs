namespace HashDog
{
    public enum ComparisonResult
    { 
        FirstRun,
        Match,
        Mismatch,
    }

    public enum HashType
    {
        MD5,
        SHA1,
        SHA256,
        SHA512,
    }
}
